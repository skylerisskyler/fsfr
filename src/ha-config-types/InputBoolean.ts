export interface InputBooleanProps {
  id?: string
  name: string;
  icon: string;
  initial?: boolean;
}

export interface InputBoolean extends InputBooleanProps {
  id?: string
  name: string;
  icon: string;
  initial: boolean;
}