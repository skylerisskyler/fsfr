lights:
  - id: light.skyler_room
    layers:
      # - context: one
      #   style:
      #     color: red
      - context: first
        style:
          color: green
      - context: second
        style:
          color: blue
          brightness: $bri
variables:
  - namespace: bri
    type: brightness
    unit: percentage